<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <application
        android:name=".db.MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KnowledgeManagerSystems">

        <provider
            android:name="com.google.android.gms.ads.MobileAdsInitProvider"
            android:authorities="${applicationId}.com.google.android.gms.ads.MobileAdsInitProvider"
            android:enabled="false"
            android:exported="false"
            tools:replace="android:authorities" />

        <activity
            android:name=".controller.EquipmentListActivity"
            android:exported="true"
            android:label="Danh sách Thiết bị">


        </activity>

        <activity
            android:name=".controller.LoginController"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".controller.ProtocolActivity"
            android:exported="false"
            android:label="Protocol / SOP Repository"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <activity
            android:name=".controller.CreateExperimentActivity"
            android:exported="false" />

        <activity
            android:name=".controller.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".controller.EquipmentDetailActivity"
            android:exported="false"
            android:label="Chi tiết Thiết bị" />

        <activity
            android:name=".controller.EquipmentBookingActivity"
            android:exported="false"
            android:label="Đặt lịch Thiết bị" />

        <activity
            android:name=".controller.AddNewProtocolActivity"
            android:exported="false"
            android:label="Add New Protocol" />

        <activity android:name=".controller.ViewProtocolActivity"
            android:exported="true"/>

        <activity android:name=".controller.SDSLookupActivity"
            android:exported="true"
            android:label="SDS Lookup" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>


    </application>

</manifest>